Ctrl-a h → show QEMU help.

Ctrl-a x → exit QEMU.

Ctrl-a c → go to QEMU monitor (low-level debug interface).

Ctrl-a then ? → similar help, depending on build.

---

<interface type='bridge'>
  <source bridge='br-vms'/>
</interface>
