version: 2
ethernets:
  ens3:
    dhcp4: false
    addresses:
      - {vm_ip}/{vm_prefix}
    routes:
      - to: 0.0.0.0/0
        via: {vms_gateway}
    nameservers:
      addresses: [{dns_servers}]
