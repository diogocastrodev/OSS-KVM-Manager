meta {
  name: Kill VM
  type: http
  seq: 6
}

post {
  url: {{PROTOCOL}}://{{IP}}:{{PORT}}/api/v1/vms/{{Example_UUID}}/status/kill
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
