meta {
  name: Fetch Specific VM
  type: http
  seq: 2
}

get {
  url: {{PROTOCOL}}://{{IP}}:{{PORT}}/api/v1/vms/{{Example_UUID}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
