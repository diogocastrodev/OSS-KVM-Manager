meta {
  name: Delete VM
  type: http
  seq: 5
}

delete {
  url: {{PROTOCOL}}://{{IP}}:{{PORT}}/api/v1/vms/{{Example_UUID}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
