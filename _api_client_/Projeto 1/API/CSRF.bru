meta {
  name: CSRF
  type: http
  seq: 1
}

get {
  url: {{API_ENDPOINT}}/api/v1/csrf
  body: none
  auth: inherit
}

script:post-response {
  let csrfToken = res.body.token;
  bru.setGlobalEnvVar("api_csrf_token", csrfToken);
}

settings {
  encodeUrl: true
  timeout: 0
}
