meta {
  name: Try get Info
  type: http
  seq: 4
}

post {
  url: {{API_ENDPOINT}}/api/v1/admin/servers/try-info
  body: json
  auth: inherit
}

headers {
  x-csrf-token: {{CSRF_TOKEN}}
}

body:json {
  {
    "server_endpoint": "100.91.240.67:5000"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
