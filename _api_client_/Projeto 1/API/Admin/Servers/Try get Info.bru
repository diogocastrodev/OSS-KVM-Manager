meta {
  name: Try get Info
  type: http
  seq: 4
}

post {
  url: {{API_ENDPOINT}}/api/v1/servers/try-info
  body: json
  auth: inherit
}

headers {
  x-csrf-token: {{api_csrf_token}}
}

body:json {
  {
    "server_endpoint": "127.0.0.1:20256"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
